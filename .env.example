# Firebase Configuration
FIREBASE_PROJECT_ID=chat-msg-64e0b

# JWT Secret (use a strong, random string in production)
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production

# Frontend URL (for CORS)
FRONTEND_URL=*

# Environment
NODE_ENV=production
